#include "raylib_binaryview.h"
#include <string>

// Simplified binary view for Raylib implementation
BinaryView::BinaryView(RaylibWidget* parent) {
  // Initialize with parent if needed
}

void BinaryView::showEvent(const std::string& route, int start_time, int end_time) {
  // Simplified implementation that would handle showing binary data
  // In a full implementation, this would render binary data visualization using Raylib
}

void BinaryView::selectItem(int item_id) {
  // Select an item in the binary view
}