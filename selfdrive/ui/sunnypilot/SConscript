# Raylib-based sunnypilot sources
# sunnypilot extends the main UI which is already Raylib-based, so we only need the core files
sp_widgets_src = [
  "sunnypilot/ui.cc",
]

sp_raylib_src = [
  "sunnypilot/ui.cc",
]

sp_raylib_util = []

Export('sp_widgets_src', 'sp_raylib_src', "sp_raylib_util")
